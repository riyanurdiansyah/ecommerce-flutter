import 'package:flutter/material.dart';

class RegiisterPage extends StatelessWidget {
  const RegiisterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
